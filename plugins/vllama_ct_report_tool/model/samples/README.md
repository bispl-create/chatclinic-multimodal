# MS-VLM CT Report Samples

Place optional local NIfTI examples for `ms_vlm_ct_report_tool` in this directory.

Example filename used in the tool README:

- `brainct_example.nii.gz`

Sample volumes are intentionally ignored by git and should be shared outside the pull request.
